         <div class="row-mt-5">
            <div class="col-8 offset-0" >
                <h2 class="text-left">Σχόλια</h2>
            </div>
        </div>
 <form action="/comment/{{theTitle}}" method="post" >
    {{!-- <div class="container"> --}}

        <fieldset class="form-group border p-3">
            <legend class="w-auto px-2">Προσθέστε το σχόλιό σας</legend>
        <div class="form-group row">
    <label for="staticTitle" class="col-sm-2 col-form-label">Τίτλος</label>
    <div class="col-sm-10">
      <input type="text" readonly class="form-control-plaintext" id="staticTitle" name="staticTitle"value="{{theTitle}}">
    </div>
  </div>
  <div class="form-group row">
    <label for="staticAuthor" class="col-sm-2 col-form-label">Συγγραφέας</label>
    <div class="col-sm-10">
      <input type="text" readonly class="form-control-plaintext" id="staticAuthor" value="{{myBook.author}}">
    </div>
  </div>
  <div class="form-group row">
    <label for="comment" class="col-sm-2 col-form-label">Σχόλιο</label>
    <div class="col-sm-10">
      <textarea  class="form-control" id="comment" name="comment" value="comment">{{comment.comment}}</textarea>
    </div>
  </div>
        {{!-- <fieldset class="border p-2 mb-3"> --}}
            {{!-- <legend class="m-auto">User Login</legend> --}}
            {{!-- <div class="row mt-1">
                <div class="col-6 offset-3">
                    <div class="form-floating">
                        <input type="text" class="form-control {{#if message.username }} is-invalid {{/if}}" 
                        value="{{message.username.value}}" name="username" id="form_username" 
                        placeholder="Όνομα Χρήστη" required="required autofocus"/>
                        <label for="form_username" class="col-form-label">Όνομα Χρήστη</label>
                        <div class="valid-feedback">OK</div>
                        <div class="invalid-feedback">{{message.username.msg}}</div>
                    </div>
                </div>
            </div>
            <div class="row mt-3">
                <div class="col-6 offset-3">
                    <div class="form-floating">
                        <input type="password" class="form-control {{#if message.password }} is-invalid {{/if}}" 
                        value="{{message.password.value}}" name="password" id="password" 
                        placeholder="Συνθηματικό" required="required autofocus"/>
                        <label for="password" class="col-form-label">Συνθηματικό</label>
                        <div class="valid-feedback">OK</div>
                        <div class="invalid-feedback">{{message.password.msg}}</div>
                    </div>
                </div>
            </div>
            {{#if newusermessage}}
                <div class="row mt-5">
                    <div class="col-6 offset-3">
                        <div class="alert alert-primary" role="alert">
                            {{newusermessage}}
                        </div>
                    </div>
                </div>
            {{/if}} --}}
        {{!-- </fieldset> --}}
        </fieldset>
        <div class="row mt-5">
            <div class="col-6 offset-1">
                <button type="submit" class="btn btn-primary login-btn btn-block ">Υποβολή</button>
            </div>
        </div>
    {{!-- </div> --}}
    

</form> 

         <div class="row-mt-5">
            <div class="col-8 offset-0" >
                <h2 class="text-left">Άλλοι Είπαν ..</h2>
            </div>
        </div>

        <table class="table">
  <thead class="thead-dark">
    <tr>
      <th scope="col">Μέλος</th>
      <th scope="col">Σχόλιο..</th>
    </tr>
  </thead>
  <tbody>
     {{#each comments}}
    <tr>
        <td>{{this.UserName}}</td>
        <td>{{this.comment}}</td>
    </tr>
     {{/each}} 
  </tbody>
</table>

</table>